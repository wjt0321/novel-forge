# 已清理书库的工作流样本

本目录保存 **2026-07-29** 清理本地 `books/` 工作区前提取的脱敏汇总样本。它是工作流
回归与基准设计的范本/反例索引，不是小说资料库，也不能用于恢复原项目。

- `catalog.json`：机器可读的分类和文件级聚合指标。
- 成功样本：K3-H、K3-L、K3-M；依据用户标注，代表工作流理念被贯彻。这里**不评价文风**。
- 失败样本：DS、M3；依据用户标注，代表流程曾绕过步骤或陷入循环。这里**不评价文风**。
- 对照样本：其余未归入以上两类的测试书；不作为工作流或文风的正、反例。

样本刻意不包含正文、标题、slug、规划内容、审稿/证据正文、哈希、运行身份、模型信息、
Guardian 材料或本地 Git 对象。尤其不能用持久化的 `ready` 标签反推历史流程正确：它可能正是
控制面绕过的反例。清理后，`books/` 仅保留其项目说明，新的实验必须重新初始化。
